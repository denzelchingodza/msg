# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets — never commit
backend/.secret
.env*

# Photos, placed images, and court.jpg ARE committed now so the deployed
# site (Vercel) actually has its images. Only the preview PNGs stay out.
frontend/public/photos_*_prev.png

# Audio IS committed now so the deployed site has sound.
# (Note: only do this if the clips' license permits redistribution.)

# OS / IDE noise
.DS_Store
Thumbs.db
.idea/
.vscode/
